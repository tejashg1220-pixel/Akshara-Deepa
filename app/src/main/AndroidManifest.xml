<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".AksharaApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_lamp"
        android:label="Akshara-Deepa Tutor"
        android:roundIcon="@drawable/ic_lamp"
        android:supportsRtl="true"
        android:theme="@style/Theme.AksharaDeepa">
        <receiver
            android:name=".receivers.ReminderReceiver"
            android:exported="false" />
        <activity android:name=".activities.ChapterDetailActivity" />
        <activity
            android:name=".activities.QuizActivity"
            android:screenOrientation="portrait" />
        <activity android:name=".activities.MainActivity" />
        <activity
            android:name=".activities.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
